import { Outlet, NavLink, Navigate } from 'react-router-dom';
import { BarChart3, Boxes, FileText, LayoutDashboard, Palette, ReceiptIndianRupee, Settings, Users } from 'lucide-react';
import { Logo } from '../components/Logo'; import { ThemeToggle } from '../components/ThemeToggle'; import { useAppStore } from '../store/useAppStore';
const links=[['Dashboard','/dashboard',LayoutDashboard],['Invoices','/dashboard/invoices',ReceiptIndianRupee],['Quotations','/dashboard/quotations',FileText],['Customers','/dashboard/customers',Users],['Products','/dashboard/products',Boxes],['Analytics','/dashboard/analytics',BarChart3],['Templates','/dashboard/templates',Palette],['Settings','/dashboard/settings',Settings]] as const;
export function DashboardLayout(){ const {token,user}=useAppStore(); if(!token) return <Navigate to="/login" replace/>; return <div className="min-h-screen bg-[#f7f8fc] dark:bg-zinc-950"><aside className="fixed inset-y-0 left-0 hidden w-64 border-r border-zinc-200 bg-white p-5 lg:block dark:border-zinc-800 dark:bg-zinc-900"><Logo/><nav className="mt-9 space-y-1">{links.map(([label,to,Icon])=><NavLink key={to} to={to} end={to==='/dashboard'} className={({isActive})=>`flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-semibold transition ${isActive?'bg-indigo-50 text-[#635bff] dark:bg-indigo-500/10':'text-zinc-500 hover:bg-zinc-100 dark:hover:bg-zinc-800'}`}><Icon size={18}/>{label}</NavLink>)}</nav></aside><div className="lg:pl-64"><header className="no-print flex h-16 items-center justify-between border-b border-zinc-200 bg-white px-5 dark:border-zinc-800 dark:bg-zinc-900"><div><p className="text-xs text-zinc-400">Workspace</p><p className="text-sm font-semibold">{user?.name || 'InvoicePilot'}</p></div><ThemeToggle/></header><main className="p-5 md:p-8"><Outlet/></main></div></div> }
