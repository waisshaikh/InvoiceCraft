import { useState } from 'react'; import { useForm } from 'react-hook-form'; import { zodResolver } from '@hookform/resolvers/zod'; import { z } from 'zod'; import { Link, Navigate, useNavigate } from 'react-router-dom'; import { Logo } from '../components/Logo'; import { api } from '../api/client'; import { useAppStore } from '../store/useAppStore';
const schema=z.object({name:z.string().min(2).optional(),email:z.email(),password:z.string().min(8)}); type Form=z.infer<typeof schema>;
export function AuthPage({mode}:{mode:'login'|'register'}){const {token,setSession}=useAppStore(); const navigate=useNavigate(); const [error,setError]=useState(''); const {register,handleSubmit,formState:{errors,isSubmitting}}=useForm<Form>({resolver:zodResolver(schema)}); if(token)return <Navigate to="/dashboard" replace/>; const submit=async(values:Form)=>{try{setError(''); const response=await api<{data:{token:string,user:{id:string,name:string,email:string}}}>(`/auth/${mode}`,{method:'POST',body:JSON.stringify(values)}); setSession(response.data.token,response.data.user);navigate('/dashboard')}catch(e){setError(e instanceof Error?e.message:'Unable to continue')}}; return <main className="grid min-h-screen place-items-center bg-[radial-gradient(circle_at_top,#eceaff,transparent_45%)] px-4 dark:bg-zinc-950"><div className="glass w-full max-w-md rounded-3xl p-7"><Link to="/"><Logo/></Link><h1 className="mt-8 text-3xl font-bold tracking-tight">{mode==='login'?'Welcome back':'Create your account'}</h1><p className="mt-2 text-sm text-zinc-500">{mode==='login'?'Sign in to sync your documents.':'Start creating professional documents for free.'}</p><form className="mt-7 space-y-4" onSubmit={handleSubmit(submit)}>{mode==='register'&&<label className="block text-sm font-medium">Full name<input className="field mt-1" {...register('name')}/><span className="text-xs text-red-500">{errors.name?.message}</span></label>}<label className="block text-sm font-medium">Email<input className="field mt-1" type="email" {...register('email')}/><span className="text-xs text-red-500">{errors.email?.message}</span></label><label className="block text-sm font-medium">Password<input className="field mt-1" type="password" {...register('password')}/><span className="text-xs text-red-500">{errors.password?.message}</span></label>{error&&<p role="alert" className="rounded-xl bg-red-50 p-3 text-sm text-red-600">{error}</p>}<button disabled={isSubmitting} className="btn-primary w-full">{isSubmitting?'Please wait…':mode==='login'?'Sign in':'Create account'}</button></form><p className="mt-6 text-center text-sm text-zinc-500">{mode==='login'?<>New here? <Link className="font-semibold text-indigo-600" to="/register">Create an account</Link></>:<>Already registered? <Link className="font-semibold text-indigo-600" to="/login">Sign in</Link></>}</p></div></main>}
